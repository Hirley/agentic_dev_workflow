module Ports
  # Porta (interface) para persistência de <%= class_name %>. Implementações
  # concretas (adapters) devem responder a estes métodos.
  class <%= class_name %>Repository
    def find(id)
      raise NotImplementedError
    end

    def save(<%= entity_name %>)
      raise NotImplementedError
    end

    def all
      raise NotImplementedError
    end
  end
end
