version: '3.8'

# Stack de observabilidade (Prometheus + Grafana). Suba junto com o
# docker-compose.yml principal:
#   docker compose -f docker-compose.yml -f docker-compose.observability.yml up

services:
  prometheus:
    image: prom/prometheus:latest
    volumes:
      - ./observability/prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - '9090:9090'

  grafana:
    image: grafana/grafana:latest
    depends_on:
      - prometheus
    environment:
      # Defina GRAFANA_ADMIN_PASSWORD no seu .env antes de subir esta stack
      # fora do localhost — sem senha fixa insegura por padrão.
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:?defina GRAFANA_ADMIN_PASSWORD no seu .env}
    ports:
      - '3001:3000'
    volumes:
      - grafana_data:/var/lib/grafana

volumes:
  grafana_data:
